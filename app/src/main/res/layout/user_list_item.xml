<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <TextView
            android:id="@+id/userNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Username"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/userEmailTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/userNameTextView"
            android:text="Email" />

        <TextView
            android:id="@+id/userSubscriptionTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/userEmailTextView"
            android:text="Subscription: 0"
            android:textColor="@android:color/holo_green_dark" />

        <!-- Eye Icon -->

        <ImageView
            android:id="@+id/viewUserIcon"
            android:layout_width="43dp"
            android:layout_height="40dp"
            android:layout_alignParentTop="true"
            android:layout_marginTop="9dp"
            android:layout_marginEnd="9dp"
            android:layout_toStartOf="@+id/deleteUserIcon"
            android:contentDescription="View User"
            android:src="@drawable/ic_info"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/deleteUserIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@android:drawable/ic_delete"
            android:contentDescription="Delete User"
            android:padding="8dp" />
    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>